{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "python",
            "request": "launch",
            "preLaunchTask": "Clear Logs",
            "program": "${file}",
            "console": "integratedTerminal"
        }
    ]
}
