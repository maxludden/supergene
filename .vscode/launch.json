{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "debugpy",
            "request": "launch",
            "preLaunchTask": "Clear Logs",
            "program": "${file}",
            "console": "integratedTerminal"
        }
    ]
}
